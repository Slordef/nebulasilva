networks:
  app:
    name: nebulasilva_app

services:
  app:
    container_name: nebulasilva_app
    build:
      context: ../..
      dockerfile: docker/production/Dockerfile
    networks:
      - app